// first notation to define arrays
// array with same types so here it storres only number type so we cant store any other type
const numbs : number[] = [1,2,3,4]
console.log(numbs)
const str : string[] = ["Cha","du"]
console.log(str)

const items :string[] =[]
items.push('keys');
// items.push (12) doesnt work
console.log(items)
items.push('lock')
console.log(items)

const numList: number[] =[]
numList.push(20)
console.log(numList)

const things: Array<number> = [1,2,3]
console.log(things)

// Multi dimensional arrays with many diff arrays 

const multiDi:number [][] = [[1,2,3,4,4]]
const tripleDi: number[][][] = [[[3,4,4,5]]]

console.log(multiDi)
console.log(tripleDi)

